import { StyleSheet } from "react-native";
import { Grid } from "../../theme/constants";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    paddingTop: Grid.titanic + Grid.huge,
  },
});
